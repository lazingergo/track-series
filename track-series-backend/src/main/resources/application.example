# Optional develop profile template.
# Copy values into application-develop.properties if you need machine-specific overrides.

# Example local database override:
# spring.datasource.url=jdbc:mysql://localhost:3306/track_series?serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=your-local-password

# Example local JWT override:
# application.security.jwt.secret-key=dev-secret-key-change-me
